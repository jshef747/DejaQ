# Copy to .env.local:  cp .env.local.example .env.local

# Backend gateway base URL (server-side proxy target)
DEJAQ_API_BASE_URL=http://127.0.0.1:8000

# Org API key — generate one in the dashboard (Keys) or via:
#   cd server && uv run dejaq-admin key generate --org <slug>
DEJAQ_API_KEY=
